# Browser requests use Vite's local proxy, avoiding CORS while developing.
VITE_API_BASE_URL=/api

# Use http://localhost:3000 for a local API, or a Render URL for a deployed API.
VITE_API_PROXY_TARGET=http://localhost:3000
